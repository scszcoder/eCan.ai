# Build system script dependencies
# Used by GitHub Actions workflows for S3 upload and appcast generation

# AWS SDK for Python
boto3>=1.34.0

# YAML parser
pyyaml>=6.0.1

# Version parsing (for semantic version comparison in latest.json generation)
packaging>=23.0
