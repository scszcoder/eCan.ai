# ============================================================================
# Build system script dependencies (Intl variant)
# ============================================================================
# Used by GitHub Actions workflows for Intl S3 upload and appcast generation.
#
# CN variant: uses Tencent Cloud COS instead of S3. Install with:
#   pip install -r requirements-cn.txt
# ============================================================================

# AWS SDK for Python (S3 upload)
boto3>=1.34.0

# YAML parser (OTA config)
pyyaml>=6.0.1

# Version parsing (semantic version comparison in latest.json generation)
packaging>=23.0
