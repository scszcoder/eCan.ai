# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Vite
.vite
.vite-ssg-manifest.json
vite.config.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
# 新架构：CN/Intl 不再使用构建期 .env.cn/.env.intl，运行时配置由后端提供
# （desktop 走 IPC handler getAppConfig，web 部署走 web_server.py 同源 /api/config）
# .env 是桌面开发期共享默认（tracked）
# .env.example 是变量文档模板（tracked）
# .env.local 是个人覆盖（gitignored）
.env.local
!.env
!.env.example

# Build files
*.tsbuildinfo
package-lock.json