# ============================================================================
# Authentication Configuration - Fallback for legacy/dev callers only
# ============================================================================
# AUTH HAS MOVED:
#   - Intl app: apps/intl/config/auth_config.yml (+ env vars AWS_COGNITO_*)
#   - CN app:   apps/cn/config/auth_config.yml   (+ env vars ECAN_TENCENT_*)
# This file is kept ONLY as a last-resort fallback for code that imports
# auth.auth_config before utils.app_config_loader is available (early startup).
# All sensitive values must be left empty here. Production credentials belong
# in environment variables or in your local .env (gitignored).
#
# To populate locally for debugging, copy this file's structure into a
# .gitignored location and set values via env vars (see docs/SECRETS_CONFIG.md).
# ============================================================================

COGNITO:
  USER_POOL_ID: ""
  CLIENT_ID: ""
  CLIENT_SECRET: ""
  IDENTITY_POOL_ID: ""
  REGION: ""
  DOMAIN: ""

# Local OAuth Server Configuration (shared dev default)
GOOGLE:
  CALLBACK_URL: "http://localhost:9382/callback"

APPLE:
  # CLIENT_ID: ""
  # TEAM_ID: ""
  # KEY_ID: ""
  # PRIVATE_KEY_PATH: ""
