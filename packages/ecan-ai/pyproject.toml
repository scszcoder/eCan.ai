[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ecan-ai"
version = "0.1.0"
description = "AI Agent Framework for Automation - Build and run intelligent agents"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [
    { name = "eCan.ai Team" }
]
keywords = [
    "ai",
    "agents",
    "automation",
    "llm",
    "langchain",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Core dependencies - minimal set for the library
dependencies = [
    "sqlalchemy>=2.0.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
# Full dependencies for running agents
full = [
    "langchain>=0.1.0",
    "langchain-openai>=0.0.5",
    "langchain-anthropic>=0.1.0",
    "browser-use>=0.11.0",
    "aiohttp>=3.9.0",
]

# Desktop GUI support
desktop = [
    "PySide6>=6.6.0",
]

# Development dependencies
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/ecan-ai/ecan-ai"
Documentation = "https://docs.ecan.ai"
Repository = "https://github.com/ecan-ai/ecan-ai"

[project.scripts]
# CLI entry point (optional, for future use)
# ecan = "ecan_ai.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ecan_ai"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.black]
line-length = 100
target-version = ["py312", "py313", "py314"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]
